<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Wired.chat</a>

    <div class="navbar-nav me-auto mb-2 mb-lg-0"></div>

    <div class="d-flex">
      <%= button_to(logout_path, class: 'btn', method: :delete) do %>
        <i class="icon mdi mdi-logout"></i>
        <span>Logout</span>
      <% end if current_user %>
    </div>
  </div>
</nav>
